# Changelog

## [Unreleased]

### Added

-   Initialized project as a Progressive Web App (PWA).
-   Added `vite-plugin-pwa` to generate a service worker for offline capabilities.
-   Used `@vite-pwa/assets-generator` to create PWA icons from a source SVG.
-   Configured the web app manifest (`manifest.webmanifest`).

### Changed

-   Updated `vite.config.js` to include PWA plugin and asset generation.
